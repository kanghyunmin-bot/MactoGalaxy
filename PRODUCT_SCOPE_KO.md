# 제품 컷 정리

## 목표

이 제품은 다음 형태로 구현한다.

- macOS 앱 1개
- Android 앱 1개
- Mac과 Galaxy Tab S11을 USB-C to USB-C로 직접 연결
- 두 앱을 실행하면 4자리 숫자 입력 방식으로 최초 페어링
- 이후에는 신뢰 기기로 저장되어 자동 재접속
- Mac에서 Android 제어
- 양방향 클립보드 동기화
- 앱 내부에서 간단한 클립보드 히스토리 조회 및 재복사

이 제품은 화면 미러링 제품이 아니다. 사용자는 Galaxy Tab 화면을 직접 본다.

## 지원 범위

### macOS

- 하나의 유니버설 앱으로 배포
- `arm64`와 `x86_64` 둘 다 지원
- Apple Silicon Mac 지원
- M5 Mac 포함
- Intel Mac 지원
  - 단, Apple이 현재 지원하는 Intel Mac 범위로 제한
  - 현실적으로는 macOS Tahoe 26 호환 Intel Mac 기준으로 잡는 것이 안전

### Android

- 1차 타깃: `Galaxy Tab S11`, `Galaxy Tab S11 Ultra`
- USB-C 데이터 연결 필수
- One UI / Android 세부 빌드는 실제 기기 프로토타입으로 고정

## 연결 및 페어링 UX

### 최초 연결

1. Mac 앱 실행
2. Android 앱 실행
3. USB-C to USB-C 연결
4. 양쪽 앱이 연결 가능 상태 진입
5. 양쪽 앱에 4자리 숫자 입력 UI 표시
6. 사용자가 같은 4자리 숫자를 양쪽에 입력
7. 검증 성공 시 페어링 완료
8. 양쪽 기기에 신뢰 상태 저장

### 이후 연결

- 같은 두 기기라면 재페어링 없이 자동 인증
- 케이블 재연결, 절전 복귀, 앱 재실행 시 자동 재접속 시도
- 단, 사용자가 신뢰 해제하면 다시 4자리 입력 필요

### 중요한 보안 설명

- 4자리 숫자는 사용자 확인용 UX다
- 실제 보안은 장기 기기 키와 세션 키 교환으로 처리한다
- 즉 4자리 숫자만으로 보안을 구성하면 안 된다

## 입력 제어

- Mac 포인터가 설정된 화면 가장자리에 닿으면 Android 제어 모드 진입
- Mac 키보드와 트랙패드/마우스 입력을 Android로 전달
- 종료는 Mac 전용 단축키 우선
- 연결 끊김 시 즉시 Mac 제어로 복귀

## 클립보드 지원 범위

### 반드시 지원

- 텍스트
- URL
- 이미지
- 영상
- 일반 파일

### 구현 방식

- 텍스트와 URL은 작은 인라인 데이터로 처리
- 이미지, 영상, 파일은 리소스 전송 + URI 기반으로 처리
- Android는 `ClipData + content:// URI + ContentProvider` 기반으로 재구성

### 현실적 제한

- 영상과 파일은 “복사/붙여넣기”가 앱마다 다르게 동작할 수 있다
- 일부 Android 앱은 파일/영상 클립보드를 paste 대신 share처럼 취급할 수 있다
- 따라서 “모든 앱에서 완전히 동일하게 붙여넣기”는 약속하면 안 된다

## 클립보드 히스토리

### 목표 UX

- 양쪽 앱에서 최근 클립보드 항목 목록 제공
- 가볍고 빠르게 보여야 함
- 항목 클릭 시 현재 로컬 클립보드로 다시 복사

### 표시 방식

- 텍스트: 앞부분 미리보기
- 이미지: 썸네일
- 영상: 썸네일 + 재생시간 또는 파일형식
- 파일: 파일명 + 크기 + 형식

### 저장 정책

- 최근 30개 기본
- 텍스트는 일부 inline 저장
- 이미지/영상/파일은 전체 무한 저장 금지
- 썸네일/메타데이터 중심 저장
- 실제 바이너리 캐시는 TTL + LRU로 정리

## 권한 및 전제조건

### macOS

- Accessibility
- Input Monitoring

### Android

- AccessibilityService
- InputMethodService를 companion keyboard로 활성화
- Foreground service
- MVP 단계에서는 USB debugging 필요

## 권장 제품 단계

### 1단계

- USB-C 직결
- ADB 기반 MVP
- 4자리 페어링
- 신뢰 저장
- 텍스트/URL/이미지 클립보드
- 기본 히스토리

### 2단계

- AOA 기반 프로덕션 USB 시도
- 이미지 안정화
- 히스토리 개선

### 3단계

- 영상/파일 클립보드
- 영상/파일 히스토리
- 대용량 전송 안정화

## 절대 과장하면 안 되는 문구

- 모든 Intel Mac 완벽 지원
- ADB 없는 USB 직결 100% 보장
- 모든 Android 앱 완벽 제어
- 모든 앱에서 영상/파일 paste 동일 보장
- 기본 키보드 변경 없이 Android 자동 클립보드 완전 지원

## 현재 기준 추천 결론

당장 실행 가능한 현실적인 제품 컷은 다음이다.

- Mac 유니버설 앱 1개
- Galaxy Tab S11 전용 Android 앱 1개
- USB-C 직결
- 최초 4자리 숫자 입력 페어링
- 이후 상시 신뢰 저장
- 텍스트/이미지 우선 안정화
- 영상/파일은 같은 구조로 확장
- 히스토리는 처음부터 구조를 넣되, 대용량 미디어는 메타데이터 중심으로 관리
